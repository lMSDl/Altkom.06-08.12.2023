<Window
    x:Class="WpfApp.Exercise"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfApp"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Exercise"
    Width="800"
    Height="450"
    mc:Ignorable="d">


    <DockPanel Background="Gray">
        <Button Margin="10" DockPanel.Dock="Bottom">Button</Button>
        <Button Margin="10" DockPanel.Dock="Top">Button</Button>
        <Grid Margin="10" Background="DarkGray">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button
                Grid.Column="0"
                Margin="10"
                Background="Azure">
                Button
            </Button>
            <StackPanel
                Grid.Column="1"
                Margin="10"
                VerticalAlignment="Center"
                Background="White">
                <Button Margin="10" Background="Red">Button</Button>
                <Button Margin="10" Background="Green">Button</Button>
                <Button Margin="10" Background="Blue">Button</Button>
            </StackPanel>
        </Grid>
    </DockPanel>

</Window>
